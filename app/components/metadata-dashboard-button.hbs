{{#if (and (not-eq this.fabricaDeployTarget "test") this.metadataDashboardUrl)}}
  <BsButton class="btn-warning metadata-dashboard-button"  id="metadata-dashboard-button" {{on "click" (fn this.openLink this.metadataDashboardUrl)}}>
    <i class="fas fa-external-link-alt"></i>Metadata Dashboard
  </BsButton>
{{/if}}
